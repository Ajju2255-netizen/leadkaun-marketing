# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# cloudflare workers
.open-next/
.wrangler/
.dev.vars
worker-configuration.d.ts

# cache-warming runtime artifacts
scripts/.warm-state/

# confidential strategy/audit docs — this repo is PUBLIC; these live in the vault only
SEARCH-GROWTH-MASTER-PLAN.md
SEO-AIO-GEO-ANALYSIS-*.md
SEO-AIO-GEO-ANALYSIS-*.pdf

# Phase 1 index tracker — generated inventory + deindex/consolidate strategy (vault only)
INDEX-TRACKER.md
/data/index-decisions.json
/data/gsc/
/data/index-snapshots.jsonl

# Phase 2 keyword->URL ownership map (architecture/strategy — vault only)
KEYWORD-URL-OWNERSHIP.md

# Phase 2.2 flagship build spec (strategy — vault only)
FLAGSHIP-2.2-BUILD-SPEC.md

# Phase 2.2 evidence ledger (competitor sourcing — vault only)
FLAGSHIP-EVIDENCE-LEDGER.md
